package domain

import (
	domainerr "{{.GoModule}}/internal/shared/errors"
)

// Module-specific domain errors.
var (
	Err{{.NameTitle}}NotFound = domainerr.New(domainerr.CodeNotFound, "{{.Name}} not found")
	ErrNameRequired          = domainerr.New(domainerr.CodeInvalidArgument, "name is required")
)
