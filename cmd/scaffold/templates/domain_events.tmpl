package domain

import "{{.GoModule}}/internal/shared/events/contracts"

// Re-export event topics from shared contracts so module code compiles unchanged.
// External modules (audit, notification) should import from contracts directly
// to avoid cross-module coupling.
const (
	Topic{{.NameTitle}}Created = contracts.Topic{{.NameTitle}}Created
	Topic{{.NameTitle}}Updated = contracts.Topic{{.NameTitle}}Updated
	Topic{{.NameTitle}}Deleted = contracts.Topic{{.NameTitle}}Deleted
)

type {{.NameTitle}}CreatedEvent = contracts.{{.NameTitle}}CreatedEvent
type {{.NameTitle}}UpdatedEvent = contracts.{{.NameTitle}}UpdatedEvent
type {{.NameTitle}}DeletedEvent = contracts.{{.NameTitle}}DeletedEvent

// EventSchemaVersion is the current event schema version for {{.Name}} domain events.
const EventSchemaVersion = contracts.{{.NameTitle}}EventSchemaVersion
