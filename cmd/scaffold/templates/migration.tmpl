-- +goose Up
CREATE TABLE {{.NamePlural}} (
    id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name       VARCHAR(255) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    deleted_at TIMESTAMPTZ
);

CREATE INDEX idx_{{.NamePlural}}_name ON {{.NamePlural}} (name) WHERE deleted_at IS NULL;
CREATE INDEX idx_{{.NamePlural}}_active ON {{.NamePlural}} (id) WHERE deleted_at IS NULL;
CREATE INDEX idx_{{.NamePlural}}_pagination ON {{.NamePlural}} (created_at DESC, id DESC) WHERE deleted_at IS NULL;

-- +goose Down
DROP TABLE IF EXISTS {{.NamePlural}};
