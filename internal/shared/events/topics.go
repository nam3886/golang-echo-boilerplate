// Package events provides the event bus infrastructure.
// Domain-specific topics and event types live in each module's domain package
// (e.g. internal/modules/user/domain/events.go).
package events
