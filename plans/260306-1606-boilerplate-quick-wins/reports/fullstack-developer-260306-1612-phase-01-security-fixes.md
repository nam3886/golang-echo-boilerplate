## Phase Implementation Report

### Executed Phase
- Phase: phase-01-security-fixes
- Plan: /Users/namnguyen/Desktop/www/freelance/gnha-services/plans/260306-1606-boilerplate-quick-wins
- Status: completed

### Files Modified
- `internal/shared/middleware/rate_limit.go` — S1: fail-closed on Redis error, log error, return 503
- `db/queries/user.sql` — S2: explicit columns in GetUserByID and ListUsers (exclude password)
- `gen/sqlc/user.sql.go` — S2: regenerated by `sqlc generate` (new GetUserByIDRow, ListUsersRow types)
- `internal/modules/user/adapters/postgres/repository.go` — S2: added toDomainFromGetRow/toDomainFromListRow helpers, updated GetByID and List callers
- `internal/modules/user/adapters/grpc/mapper.go` — S3: log raw error with slog, return generic "internal error" to client
- `internal/shared/auth/password.go` — S4: maxPasswordBytes=72 const, guard in Hash()
- `internal/shared/middleware/auth.go` — S5: blacklist check fail-closed (any Redis error rejects token)
- `internal/shared/auth/jwt.go` — S6: Issuer/Audience in GenerateAccessToken, jwt.WithIssuer/WithAudience in ValidateAccessToken
- `internal/modules/user/adapters/grpc/routes.go` — S7: RBACInterceptor moved before validate.NewInterceptor

### Tasks Completed
- [x] S1: Rate limiter fail-closed when Redis down
- [x] S2: SELECT explicit columns (exclude password_hash) + sqlc regenerate
- [x] S3: Mapper error sanitization (log + generic internal error)
- [x] S4: Max password length 72 bytes
- [x] S5: Token blacklist fail-closed
- [x] S6: JWT iss/aud claims added and validated
- [x] S7: Interceptor order RBAC before validate

### Tests Status
- Type check / build: pass (`go build ./...` clean)
- Unit tests: not run (out of scope for this phase)

### Notes
- S2: sqlc generates separate row types (GetUserByIDRow, ListUsersRow) without Password field. Two thin helpers reconstruct domain.User with empty password string — password is only needed in auth flow (GetByEmail path, unchanged).
- S6: jwtAudience hardcoded as "gnha-services" constant; Issuer sourced from cfg.AppName ("gnha-services" default). Existing tokens will fail validation after deploy — expected breaking change for security fix.
- S5: err variable from Exists() was previously discarded with `_`; now surfaces to fail-closed path.

### Issues Encountered
None.

### Next Steps
- Run integration/unit tests to confirm no regressions
- Rotate JWT_SECRET on deploy (existing tokens will have no iss/aud, will be rejected)
